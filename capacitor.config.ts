import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firstbook.app',
  appName: 'first-book-client',
  webDir: 'dist/client',
  bundledWebRuntime: false
};

export default config;
